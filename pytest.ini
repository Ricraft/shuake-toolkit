[pytest]
testpaths =
    tests
    Autovisor/tests
norecursedirs =
    .git
    .venv
    build
    data
    dist
    node_modules
    runtime_deps
    Yatori
addopts = -p no:cacheprovider --import-mode=importlib
