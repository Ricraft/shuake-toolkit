# Unified launcher + Autovisor runtime dependencies.
# Optional OpenCV/NumPy wheels live in Autovisor/runtime_deps and are listed in
# requirements-optional.txt for environments that prefer a normal pip install.

playwright>=1.52,<2
PyGetWindow>=0.0.9,<1
requests>=2.32,<3
PyYAML>=6.0.1,<7
jieba>=0.42.1,<1
pycryptodome>=3.20,<4
pywebview>=4,<5
