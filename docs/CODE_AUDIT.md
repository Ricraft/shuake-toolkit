# 代码审查与重构记录

审查日期：2026-07-16

## 结论

项目不是“完全未实现”，但不同子系统成熟度差异很大。统一启动器的大部分前端操作都有后端映射；Autovisor 已覆盖多类课程流程，且单/多账号现已共用同一执行核心；题库具备本地匹配、远程查询和 AI 兜底。`tasks.py` 巨型文件已完成职责拆分，启动器的课程目录、进程监督与配置持久化也已独立，桌面入口已统一为 WebView；主要风险集中到剩余启动器控制器代码、页面选择器脆弱、Dashboard 日志推导仍依赖文本日志而非结构化事件，以及真实平台契约测试不足。

以下比例是基于入口、UI/API 映射、真实执行路径和测试覆盖作出的工程估算，不是按代码行机械计算：

| 子系统 | 估算实现率 | 依据 |
|---|---:|---|
| 统一启动器 WebView | 80%–85% | 配置、启停、日志、题库、课程获取和偏好均有桥接；更新与系统集成功能依赖外部环境 |
| 本地题库服务 | 75%–85% | CRUD、去重、匹配、远程与 AI 路径齐全；缺少认证、并发及故障注入测试 |
| Autovisor 单账号 | 70%–80% | 普通课、共享课、见面课、测验流程已模块化并有状态测试；真实登录与播放契约仍待验证 |
| Autovisor 多账号 | 65%–75% | 已复用完整单账号核心并支持固定并发队列；真实多账号页面流程仍待验证 |
| FastAPI Dashboard | 74%–84% | 配置、真实任务进程启停/状态、子进程日志、会话隔离、去重后的日志推导统计和课程表状态已接通；仍需结构化事件与平台课程字段契约 |
| 整体（按主要使用路径加权） | 约 72%–78% | 核心路径可用，配置/进程回归覆盖已建立；站点变化仍是主要风险 |

## 本轮已修复

1. 新增 `Autovisor/runtime_bootstrap.py`，所有 Autovisor 入口在导入 `slider` 前统一挂载 `runtime_deps`。
2. 单账号和多账号在课程结束后主动取消永久监听协程，避免正常完成却无法退出；任务监控器能正确处理取消状态。
3. 启动器增加原子启动占位，阻止连续点击产生两个 Yatori/Autovisor 进程，并支持取消尚未完成的启动。
4. 刷题模式禁止重复启动，纳入“一键停止”和退出清理，并确保子进程被回收。
5. 题库设置恢复尊重 `ai_enabled`；保存时热更新 AI/自动缓存设置；运行中修改端口会重启服务；非法端口会被拒绝。
6. YAML、INI、JSON 配置改为同目录临时文件 + `os.replace` 的原子写入，降低断电/崩溃造成半文件的概率。
7. Autovisor 账号显示名称现在会持久化，不再重启后丢失。
8. 题库 HTTP 路由改为精确匹配，限制请求体大小，并拒绝非本机网页 Origin，降低恶意网页调用本地服务的风险。
9. 核心更新恢复 TLS 证书验证，增加 ZIP 越界路径检查和下载长度校验；第三方 GitHub 代理默认关闭，仅在显式设置 `LAUNCHER_ALLOW_GITHUB_PROXIES=1` 时启用。
10. 依赖定义集中到 `src/dependencies.py`；基础、可选、Web、开发依赖分组；删除了第二套已漂移的运行时下载实现。
11. pywebview API 适配器从巨型启动器拆到 `src/launcher_api.py`，配置 IO 拆到 `src/atomic_io.py`。
12. 单账号和多账号统一调用 `Autovisor.run(config_path, account_id)`；多账号模块缩减为纯进程编排器，并修复 `--max N` 导致只运行前 N 个账号的问题。
13. 运行配置统一支持未编号/编号小节、公共选项回退、账号 ID 保真、独立 Cookie 和运行时倍速热读取；后台视频任务不再私自重读默认账号。
14. 日志文件加入时间、PID 和账号标识，避免多进程覆盖；声音开关在视频恢复与全国共享课路径中保持一致。
15. 题库客户端移除导入阶段网络探测，并真正使用启动器传入的 `QB_URL`（协议、主机、端口、路径和查询参数）。
16. 增加公开配置示例、Cookie 全目录忽略规则和 pytest 收集边界；运行时 Cookie 不再作为源码维护。
17. 新增 `CourseKind`、`CourseProfile` 和 `CourseSession`，课程域名识别、标题提取、页面初始化及 `working_loop` 参数映射改为集中维护；URL 查询参数不再能误判课程类型。
18. `tasks.py` 的视频优化、播放和跳题流程改用统一课程分类，移除多处散落的域名字符串判断。
19. 使用内置浏览器对智慧树公开登录页做只读选择器审计；登录按钮选择器由命中 7 个元素的 `.wall-sub-btn` 收紧为登录面板内唯一的 `.wall-main .wall-sub-btn`，主模式与刷题模式共用选择器常量。
20. 新增无 Playwright 依赖的 `LessonNavigationState`，统一全国共享课的未完成卡片过滤、按键定位、尝试轮次和测验重试状态；修复“只剩测验时首次尝试后直接退出、配置的重试上限无法生效”的逻辑问题。
21. 新增 `NationalTestSession`，统一全国共享课的响应监听、新页面探测、开始答题、临时页面关闭和课程列表恢复；使用 `finally` 保证异常与卡片失效路径也会释放监听器和后台任务。
22. 新增 `run_hike_course` 独立流程，智慧共享课的深度扫描、逐卡学习、返回母页和重新扫描已移出主循环，并以依赖注入方式覆盖空扫描、点击失败和总时限分支。
23. 新增 `run_meeting_course` 与见面课播放器准备器，合并两段重复的倍速/播放回退代码；倍速改为首个成功打开的视频设置，控件失败会继续回退到 video.js/原生属性，部分视频点击失败时不再错误报告“所有视频已完成”。
24. 新增 `run_normal_course` 与 `NormalTestSession`，普通课视频/测验流程和时限判断移出主文件；修复测验点击失败时后台等待未取消、同页测验完成后未返回列表、状态延迟导致重复答题及不同测验共用重试次数的问题。
25. 新增 `run_national_course`，全国共享课扫描和视频段移出主文件；连续空扫描限制为 5 次、单卡片定位失败限制为 3 次，并以主机名+路径精确判断是否回到课程列表。`working_loop` 从约 900 行降为约 54 行的纯分派器。
26. 当前课程 URL 改为由主入口显式传入各执行流程，修复配置多门课程时第二门及以后课程完成测验/视频后错误返回 `course_urls[0]` 的问题。
27. 新增 `question_bank_client.py`，题库 HTTP、答案匹配和模型响应解析从 `tasks.py` 拆出；修复中文响应按 UTF-8 字节索引截取 JSON 导致错位、选项字母回退遇到空文本越界，并保留 `tasks.QB_URL` 动态覆盖兼容。
28. 新增 `video_tasks.py`，窗口激活、倍速/静音调节、暂停恢复和协程监控从 `tasks.py` 拆出；任务监控现在能报告启动监控前已经失败的任务，页面在首次加载阶段关闭也能正常下线而不再泄漏异常，并保留原模块的兼容导出。
29. 新增 `test_capture.py`，`doHomework/lookHomework` 响应监听、题目字段归一化与监听器生命周期从 `tasks.py` 拆出；重复 setup 会先释放旧监听器，未 setup/重复 cleanup 均可安全调用，并修复“响应已经到达但零超时等待仍返回失败”的竞态。
30. 新增 `answer_strategy.py`，把题库答案集中转换为索引/文本点击动作；选项点击函数现在返回真实成功状态，修复“所有点击策略均失败却仍标记答题成功并可能自动交卷”的问题，自动模式会按实际点击成功数执行 50% 安全阈值。
31. 新增 `chapter_learning.py`，章节视频识别、暂停恢复、完成等待和测验编排从 `tasks.py` 拆出；扫描与点击改用同一选择器，并修复“所有未完成目录项（包括测验）都会被误当作视频”的判断错误，视频未完成时会阻止进入测验。
32. 新增 Dashboard `TaskSupervisor`，`/api/tasks/start` 现在真实启动单/多账号入口，状态来自子进程 PID/退出码，`/api/tasks/stop` 会终止所属进程树，API 关闭也会回收任务；单课程 URL 通过 CLI 临时覆盖而不改配置，并把 Dashboard 默认监听收紧到本机。
33. `Logger` 改为固定写入 `Autovisor/logs` 的绝对路径，每条记录加锁并即时追加到文件，异常退出不再丢失整段运行日志；内存只保留最近 2,000 条兼容窗口，并支持首次写入前补充账号 ID，修复路径漂移、无界字符串复制和并发写入风险。
34. 新增 `test_page_controls.py`，把用户操作等待、选项定位、翻页与交卷控件从 `tasks.py` 拆出并保留兼容导出；修复 Playwright 回退调用名错误、答案特殊字符破坏注入脚本、翻页失败后逻辑题号继续前进，以及仅点击提交按钮就误判交卷成功的问题。
35. 手动答题的安全交卷阈值改为检查当前可见题目的真实单选/多选/文本填写状态，并在翻页时记录；修复“题库命中就算已作答、用户实际作答但题库未命中却算未答”的双向误判，页面关闭时也不会继续检测或提交。
36. 新增 `page_monitor.py`，通用精确文本点击、页面状态探测、上下文重建和安全验证监控从 `tasks.py` 拆出；文本不再直接拼接进 JavaScript/XPath，修复引号、反斜杠或换行导致脚本失效的风险，并为重复重建与验证窗口恢复补充生命周期测试。
37. 新增 `in_class_questions.py`，普通/融合/全国智慧课随堂题的提取、题库匹配、回退点击与后台监听从 `tasks.py` 拆出；题库 HTTP 查询移到工作线程，智慧课无法关闭弹窗时会先匹配题库，普通单选未命中不再连续点击两个选项。HIKE 页面只暂停自动作答监听，切换到后续课程会恢复；旧完成事件会在等待前清理，用户手动关闭题目后也能解除阻塞。
38. 新增 `test_page_flow.py`，测验批量查询、浮窗更新、逐题导航和安全交卷编排从 `tasks.py` 拆出；整份试卷改为最多 4 路有界并发查询，单题题库异常会隔离为未命中而不终止试卷，浮窗数据通过 Playwright 参数传递。`tasks.py` 从最初 2,185 行降至 123 行兼容门面，并继续支持外部覆盖题库和页面控件实现。
39. 新增 `course_catalog.py`，智慧树/学习通课程解析、账号身份缓存和学习通登录请求从统一启动器拆出。缓存现在绑定提供方+用户名摘要，同一账号序号更换用户名不会串用旧课程或 Cookies，损坏/过期/旧格式缓存会安全失效，写入使用原子替换；学习通课程 API 从 HTTP 升级为已验证可响应的 HTTPS。智慧树抓取脚本支持任意编号账号、原子保存且导入时不再自动执行，未配置账号或本轮未刷新课程文件时会拒绝读取历史数据；课程目录模块对 `requests`/`Crypto` 保持延迟导入。
40. 新增 `process_supervisor.py`，子进程输出解码、进度日志识别、运行状态占位和进程树终止从统一启动器拆出；输出读取按可用块处理并保留 `\r` 进度覆盖。Windows `taskkill` 返回非零时不再误判成功，而会回退到 `terminate/kill`；旧进程延迟退出也不能清除新进程状态，并补充并发启动占位测试。
41. 新增 `config_service.py`，Yatori/Autovisor 默认值、归一化、加载和原子保存从统一启动器拆出。INI 关闭 `%` 插值，密码与 URL 可原样往返；损坏 YAML/INI 和非法布尔值安全回退且不自动覆盖源文件。任意编号账号、每账号浏览器、未知配置段/选项和数字顺序课程 URL 均可保留；全局浏览器段不再生成虚假账号，课程获取会把界面位置映射到真实账号编号，避免 `账号-2、账号-7` 等稀疏配置抓错登录身份。Web 编辑链路也会保留账号 ID。
42. 删除统一启动器全部 Tk 页面、控件、表单同步、日志队列和降级启动分支，构造器与退出生命周期改为纯 WebView；浏览器路径选择继续使用 pywebview 文件对话框。`pywebview` 从可选依赖提升为启动器必需依赖，并增加源码契约、窗口绑定、文件选择与未确认退出测试。启动器由约 4,750 行缩减到约 3,040 行。
43. 新增 `question_bank_controller.py`，题库设置、AI 配置和本地 HTTP 服务生命周期从统一启动器拆出，Web API 通过兼容委托保持不变。API Key 仍只保存在内存或 `QB_AI_API_KEY` 环境变量中；设置写入失败会回滚内存状态，运行中修改端口会重启服务。修复服务对象已创建但端口始终未监听时仍被错误标记为运行中的问题，失败实例现在会主动清理。启动器进一步缩减到约 2,850 行。
44. 新增 `update_controller.py`，Yatori/Autovisor 版本检查、安装线程、进度日志和共享更新状态从统一启动器拆出。重复检查会被拒绝，运行中的核心禁止覆盖安装，安装异常后会可靠释放状态；Autovisor 确认已是最新版时会清除旧安装候选，避免之后误装陈旧版本。Web 动作和旧启动器方法名保持兼容，启动器进一步缩减到约 2,660 行。
45. 题库 JSON 导入/导出、SQLite 清理/去重和 ZError 数据库探测并入 `question_bank_controller.py`，WebView 入口只保留文件选择和结果提示。导入会初始化缺失数据表并兼容数组答案/选项；空库导出、空库清理不再报错。修复 SQLite 上下文管理只提交但不关闭连接导致 Windows 长期锁定题库文件的问题，所有迁移后的连接均显式关闭。启动器缩减到约 2,490 行。
46. 新增 `autovisor_dependency_manager.py`，Autovisor Python 依赖检查、镜像回退安装、Chrome/Edge 探测、Playwright Chromium 准备和配置别名同步从启动器拆出，配置改为原子写入。修复缺失依赖自动安装时把包名字符串当三元组解包而直接崩溃，以及延迟错误回调引用已释放异常对象的问题；并发安装会被可靠拦截且失败后释放状态。Web 启动动作现在会返回真实的缺配置、缺入口或缺 Python 原因，前端会展示后端失败信息；配置保存/校验失败后不再继续使用旧配置启动核心，而停止核心不再被配置校验阻断。启动器缩减到约 2,270 行。
47. Web 日常动作改为统一返回结构化结果和最新运行状态：一键启动会汇总题库、Yatori、Autovisor 的部分失败，不再无条件显示成功；题库导入/导出会区分取消、成功与失败，清理和去重错误会正确传到前端；打开配置目录和配置生成器也会报告路径缺失或未知核心。清空题库按钮增加不可撤销操作确认，避免误触直接删除数据。
48. WebView 初始化和运行状态同步增加长时间运行保护：`DOMContentLoaded` 会主动尝试连接，解决后端晚注入但未重复派发就绪事件时页面永久停在“连接中”的问题；初始化使用单飞锁和限频重试，状态轮询暂停于后台标签并禁止请求重叠。所有运行状态响应带前端递增序号，按钮操作产生的新状态不会再被更早发出的旧轮询响应覆盖；桥接暂时消失或调用失败时会显示重连状态并自动恢复。
49. 新增共享 `login_flow.py`，单账号与刷题模式不再维护两套登录实现；登录表单加载限制为 30 秒，自动登录限制为 120 秒，账号错误、验证码未完成或页面改版时会明确失败并停止任务，不再受全局 24 小时超时拖住。手动登录仍保留显式 24 小时等待；课程页面全局默认等待收紧为 30 秒。Chrome 对公开登录页复验确认面板、账号框、密码框和面板内登录按钮均唯一命中。
50. 新增共享 `course_portal.py`，刷题模式进入“我的学堂”改为有界、可验证的导航：优先点击当前首页唯一的 `onlineweb.zhihuishu.com` 链接，入口失效、首页加载失败或仍停留在测验子页面时使用课程门户直达地址，并在 20 秒内等待真实课程容器。登录重定向、课程列表未渲染和两条导航路径均失败时会明确停止刷题模式，不再继续误报“已启动”；答题后的返回结果与重新登录后的导航结果也会向主循环传递，失败时不再显示“已返回我的课堂”。同时删除原先最多约一分钟的串行选择器等待与未经验证直接返回成功的备用路径。
51. 新增 `course_queue.py`，多课程执行改为按课程隔离失败：单门课程的 HTTP 错误、选择器异常或流程错误会记录完整堆栈并继续后续课程，队列结束后汇总成功/失败数量并以非零状态报告部分失败；登录重定向和浏览器关闭仍作为致命错误立即停止。课程首次打开显式限制为 30 秒并拒绝 HTTP 4xx/5xx。后台验证、视频优化、跳题、播放、窗口激活和监控任务统一进入 `background_task_scope`，正常结束、登录失败、课程异常和浏览器中断都会在浏览器上下文关闭前取消并回收。浏览器意外关闭不再返回成功退出码，登录失效也会显示明确恢复提示。
52. 视频学习循环改为返回明确完成状态，达到学习时限、刷新失败、进度恢复耗尽或播放器持续加载错误时返回失败；普通课、翻转课、全国共享课和见面课均会传播该结果，不再把未确认完成的视频记录为“已完成”。登录在播放期间失效会升级为致命认证错误。停滞判断同时观察页面百分比和 `video.currentTime`，长视频即使整数百分比暂时不变，只要播放位置前进就不会误刷新；停滞与加载错误共享最多 3 次恢复预算，避免永久故障下无限刷新。普通课程进度条不再把 80% 强制显示为 100%，所有显示值限制在 0%–100%。
53. 普通课和全国共享课测验会检查 `handle_test_page` 的真实布尔结果，新增 `ANSWER_FAILED` 状态；答案应用或交卷未确认时不再返回“已答题”。普通课按原 5 次上限重试失败测验，全国共享课仅在 API 已确认完成或交卷已确认成功后把测验记为本轮完成，防止页面状态延迟导致重复提交。全国共享课移除“自动答题但跳过提交后立即关闭页面”的矛盾路径，改为自动提交并等待结果。手动提交交接不再冒充已提交成功；提交确认由固定等待 2 秒改为最多约 15 秒的有界页面证据轮询，兼顾慢网络并避免无限等待。
54. 新增 `runtime_activity.py`，把 Autovisor 日志归纳为启动、登录、课程、视频、测验、完成、失败和停止等结构化状态；Web 首页会显示当前课程序号/名称、真实百分比和最近失败原因。新任务写入会话边界，避免上一轮完成或失败状态污染本轮展示；URL 查询参数不会进入摘要。
55. Web“保存全部设置”现在会先快照 Yatori、Autovisor 和题库配置的原始字节与不存在状态，任一写入或题库应用步骤失败都会原子恢复全部文件，不再留下半套新配置；多账号开关也会回滚。运行中的题库切换端口失败时会恢复旧内存设置、旧配置文件、AI/自动缓存配置并重新启动旧端口，写盘后的回调异常也走同一回滚路径。
56. Web 的 AI 连通性测试和模型列表请求现在让顶层 `ok` 与真实业务结果一致，不再出现 `ok=true`、`success=false` 的矛盾响应；题库模型列表获取失败会显示后端的具体错误，不再被空数组分支误报为“请手动输入”。异步退出失败也会写入系统日志而不是静默吞掉。
57. 启动器偏好保存现在返回真实写盘结果，写入失败会恢复内存原值；开机自启脚本改用原子替换，启动目录缺失或应用失败会回滚偏好文件和启动项。Web 开关在后端拒绝时会恢复原勾选状态并显示具体错误，断连时明确提示仅临时保存在当前页面，不再让用户误以为系统级偏好已经生效。
58. 新增 `preferences_service.py`，Web 偏好的默认值、兼容加载、原子写入、值对象保真、系统副作用失败回滚和统一结果结构从启动器拆出；启动器只保留 Windows 启动项、窗口与日志清理等平台适配回调。独立服务测试覆盖损坏/扩展字段兼容、写盘失败、副作用失败及成功更新，主启动器缩减至约 2,080 行。
59. 新增 `ai_service.py`，AI 连通性测试和模型列表请求的输入归一化、延迟依赖加载、业务结果传播、异常结构与安全日志从启动器拆出。非法请求、空密钥、自定义地址缺失会在网络调用前拒绝；内置提供方仍可使用默认端点，模型返回类型会归一化，依赖/测试器异常不会破坏 Web 响应或泄露 API Key。主启动器缩减至约 1,990 行。
60. 新增 `web_action_service.py`，Web 按钮动作的分发、运行状态附加、题库结果提示和异步退出从启动器拆出；启动、停止、批量操作、更新、目录、题库与未知动作仍保持原有桥接契约。独立测试覆盖具体启动错误、批量结果保真、题库停止判定、去重成功提示和异常反馈，主启动器缩减至约 1,910 行。
61. 新增 `course_api_service.py`，智慧树/学习通课程列表的账号校验、缓存命中、Playwright 环境准备、抓取进程监督、数据读取和身份验证从启动器拆出。智慧树结果改用内容指纹确认真实刷新，末尾无换行的脚本输出不再丢失，输出管道异常会清理遗留进程，其他账号或非法顶层数据不再误报成功。主启动器缩减至约 1,730 行。
62. 新增 `python_runtime.py`，源码版与打包版共用的 Python 解释器发现逻辑从启动器拆出；Windows `py` 启动器按 3.13 至 3.9 探测，空输出、非零退出和超时可安全回退，常见安装目录按版本优先级选择。未使用且静默吞错的虚拟环境反推函数已删除，主启动器缩减至约 1,660 行。
63. 真实 WebView 冒烟发现题库页顶部显示“运行中”时，服务器卡片仍保留“服务已停止”的静态说明；`setQbStatus` 现同步更新该说明。启动自动更新还存在重复日志、默认禁用代理却仍遍历所有代理，以及网络失败时伪造过时 `v1.2.8` 版本的问题；代理候选现严格服从显式开关，Yatori 优先读取 GitHub `expanded_assets`，无法确认发行资源时返回失败而非提供降级版本。修复后已在真实 WebView 复验后端连接、题库 8083 服务、顶部与卡片运行状态一致、核心设置、Autovisor 设置、题库设置和软件设置渲染；账号密码保持掩码，刷题模式与获取课程入口存在，主题背景正常。复验只切换只读页面，未启动核心、获取课程、刷题、保存配置或清理数据。
64. 新增 `runtime_process_service.py`，Yatori 与 Autovisor 共用后台线程、进程创建、日志流、退出通知和状态回收生命周期。日志管道或后续处理异常时会检查并终止仍存活的进程树，不再只清除 Web 状态而遗留后台核心；启动前、题库准备期间取消以及线程构造/启动失败均会可靠释放启动占位。Autovisor 的题库等待改为可取消的短间隔等待，主启动器缩减到约 1,600 行。
65. Web 刷题模式也接入 `RuntimeProcessService` 的现有进程监控能力，启动状态改为原子占位，避免两个 Web 请求同时创建浏览器进程；同步创建失败、日志读取异常和监控线程失败均会清理进程并恢复状态。“一键停止”和窗口退出继续回收刷题模式及其浏览器子进程，日志仍进入 Autovisor 面板。
66. 修复 Web 多账号刷题按钮只改变卡片外观、执行端却始终读取默认账号的问题。前端现在把卡片序号传到桥接层，启动器将其映射为配置中保真的 `account_id`，`Practice_Mode.py` 通过 `--account-id` 传给共享 `Config`；登录加载与保存使用对应账号的独立 Cookie 文件，非法或已失效的卡片序号会在创建进程前明确拒绝。
67. `Practice_Mode.py` 未处理异常不再被顶层捕获后以返回码 0 伪装为正常结束；入口现在返回可测试的退出码，CLI 用 `SystemExit` 传播。共享现有进程监控会记录非零返回码，并在并非用户主动停止时向 Web 发出“运行异常”通知，使登录失败、页面改版或答题流程崩溃不再只显示“已退出”。
68. Web 账号卡的刷题按钮改为由后端 `running.practice`、`starting.practice` 和真实 `practice_account_id` 驱动，不再启动 5 秒后无条件恢复成可点击状态。当前账号可直接“停止刷题/取消启动”，其他账号在任务期间锁定；停止桥接会回收浏览器进程树，准备题库期间收到取消请求也不会继续创建浏览器。进程退出或启动失败会清除账号标识，轮询后按钮自动恢复。
69. 刷题主入口用 `finally` 显式关闭 Playwright 浏览器，正常结束、登录失败和答题循环异常不再依赖解释器退出间接回收 Chrome 子进程；浏览器已经关闭或关闭失败时会留下限量诊断日志而不覆盖原始结果。用户关闭主页面后主循环会退出并移除 `doHomework` 响应监听器。
70. 刷题模式的 session 检测/重新登录异常，以及返回“我的学堂”的第二次导航失败不再被 `except Exception: pass` 静默隐藏；诊断包含异常类型和截断原因。同一恢复点 30 秒最多记录一次，避免网络持续抖动时每秒刷屏；每次刷题会话开始时清空限频状态。
71. 删除最后一个 Tk 依赖安装界面，将 `scripts/install_dependencies.py` 改为默认只读检查、显式 `--install` 才修改环境的命令行维护入口；核心依赖与 Autovisor FastAPI 面板依赖分组管理，面板依赖仅通过 `--with-dashboard` 选择。安装过程固定使用当前或显式指定的 Python 解释器，PyPI 与 Playwright 镜像可独立配置，浏览器下载环境变量只传给子进程。新增测试约束两份 requirements 与依赖策略一致、项目维护入口不再导入 Tk，并验证 pip 命令与下载环境的传播。
72. 新增 `practice_mode_service.py`，将刷题账号解析、入口与解释器校验、题库准备、进程命令和环境构建、共享监控接入、停止与异常清理从主启动器拆出；主启动器仅保留 Web 委托，缩减至约 1,820 行。题库服务器明确返回启动失败时会释放原子启动占位并阻止浏览器进程创建，不再带着不可用题库继续刷题；新增回归覆盖失败返回、状态回收和未创建进程。
73. 核心自动更新接入 GitHub Release 资源的 `sha256` 摘要、资源大小和资源 ID 元数据；下载后在解压及替换旧核心前执行大小与 SHA-256 双重校验，使用恒定时间摘要比较。缺少摘要、摘要格式异常、大小不符或内容不符均会安全拦截，其中缺少/异常摘要会在下载前拒绝，避免浪费带宽；现有核心和配置不会被改动。GitHub 页面回退链接和源码 `zipball` 仍可报告新版本，但默认不能自动安装；仅显式设置 `LAUNCHER_ALLOW_UNVERIFIED_CORE_UPDATES=1` 才允许开发者跳过。回归覆盖元数据保真、正确摘要、摘要不符、大小不符、默认拦截、显式覆盖及旧核心保留。
74. 新增 `modules/diagnostics.py`，为可恢复的浏览器异常提供单行截断摘要和按恢复点限频的诊断器。视频学习主循环读取初始播放状态、播放位置、暂停/恢复状态以及扫描/关闭课程弹窗时，不再使用无日志的宽泛异常忽略：页面已关闭会立即传播到现有非零退出路径，普通页面抖动或选择器异常每个恢复点 30 秒最多记录一次并继续有界恢复。新增测试覆盖异常摘要、独立限频键、重置、重复位置失败只记录一次，以及页面关闭从视频位置与弹窗扫描路径传播。
75. 页面状态 OCR、安全验证监控、强制重建的上下文关闭以及精确文本回退接入共享限频诊断。已知 Playwright 选择器超时继续作为轮询空窗安静重试，页面关闭会结束对应监控；未知 DOM/上下文异常会保留异常类型与原因，并按恢复点限频，不再无限 `continue/pass`。新增测试覆盖文本回退错误细节、上下文关闭失败、重复 OCR 探测异常只记录一次、安全验证选择器变化限频和最终页面关闭下线。
76. 刷题模式的页面健康检查、题目响应等待、测验标签扫描、试卷渲染探测、空闲 URL 读取和多余标签关闭统一接入共享限频诊断。只有 Playwright 明确报告页面关闭时才结束循环，普通网络或 DOM 抖动不再被误报为“浏览器页面已关闭”；已知选择器超时继续有界重试，未知异常保留类型与原因。返回课程页和答题期间遇到页面关闭会走现有监听器清理路径，测验页关闭也会停止无意义的 60 秒渲染等待。新增回归覆盖候选页故障隔离、关闭失败可见性、页面健康误判修复、监听器异常限频和退出清理。
77. `TestResponseHandler` 的已有页面注册、新页面回调、响应地址读取、响应解析以及三类监听器释放接入共享限频诊断。页面明确关闭继续作为正常生命周期安静处理，单个标签页注册失败不会阻断其他页面，未知回调异常不再泄漏为事件循环后台错误；响应与清理错误会保留异常类型和原因，持续故障不会刷屏。新增回归覆盖已有页隔离、新页面回调、响应地址异常和关闭/清理错误分流。
78. 智慧树登录入口适配新版 `login.zhihuishu.com` 页面，并集中维护新旧两套表单契约：新版账号字段、密码字段、协议勾选框和登录按钮与旧版 `passport` 页面选择器并存。普通刷课、刷题模式、滑块流程、Web 课程获取脚本和调试脚本统一复用登录 URL 与选择器；自动登录会先确认协议勾选状态，遇到重复控件时安全停止。登录状态判断改为精确主机识别，避免将查询参数中的伪登录地址误判为会话失效；回归覆盖新版页面、旧版回退、协议勾选、歧义控件拦截和双入口配置导入。
79. 登录完成判定从“表单隐藏”收紧为“表单隐藏且已离开登录主机”，兼容服务端或单页应用延迟跳转；仍停留在登录地址会有界等待并按失败处理，不再保存无效 Cookie。Web 课程获取脚本移除旧版“我的学堂”文本点击和固定 `#sharingClassed` 子节点路径，改为复用共享课程门户导航并在门户未就绪时保留旧缓存。Chrome 只读复验确认新版面板、账号、密码、协议和提交控件均唯一命中，旧版面板与账号字段已不再出现。
80. 新增 URL 与页面 DOM 双重登录状态识别，覆盖课程域名已提交跳转但新版登录面板先完成渲染的竞态窗口；课程门户、课程打开、视频健康检查和刷题会话恢复统一使用该识别，不再在登录 DOM 上继续执行页面优化或播放轮询。课程获取脚本的完整账号改为日志脱敏，课程访问密钥只记录“已获取/未提供”状态，实际缓存数据保持不变。Chrome 对未登录课程门户的只读检查复现了该跳转顺序，回归覆盖 DOM 登录页拦截、课程优化前停止和账号脱敏。
81. Web 课程获取从独立的无头登录实现改为复用核心 `Config`、按账号 Cookie 文件和统一登录流程；已有凭证会在新上下文中加载，成功登录后写回同一账号凭证，失效或出现安全验证时打开可见浏览器供用户处理，并以 180 秒外层边界防止 Web 请求无限挂起。浏览器路径按每个账号的 Chrome/Edge 偏好排序，找不到首选项才回退另一浏览器；课程门户未就绪仍不会覆盖旧课程缓存。回归覆盖账号配置、Cookie 路径隔离、凭证注入、可见启动和浏览器偏好顺序。

82. 智慧树 Web 课程获取增加后端单实例互斥，不再仅依赖前端按钮状态防重；抓取进程纳入服务生命周期管理，重复 API 调用会返回稳定的“正在进行中”提示，“全部停止”和应用退出会发出取消并回收可见浏览器进程，依赖安装阶段收到退出请求也不会继续启动浏览器。进程输出异常、用户取消和正常结束均通过 `finally` 清空活动引用，避免陈旧进程污染下一次获取；回归覆盖并发拒绝、重复停止幂等、异常清理、退出与全部停止联动。

83. 智慧树课程抓取脚本不再把“未收到课程接口响应”当作成功：登录失败、课程门户失败、接口契约缺失和文件保存失败分别返回稳定的非零退出码，Web 后端可直接显示脚本失败而不是误读历史文件。只有确认收到成功的 `queryShareCourseInfo` 响应后才允许原子写入，页面接口变化或解析异常时保留旧课程数据；合法空课程响应仍可保存。重复课程响应按课程访问标识合并并以最新进度覆盖，避免重复卡片；课程类型和时间戳中的合法零值不再被误写成缺省符号。

84. 智慧树登录入口改为与 Chrome 当前真实重定向一致的双层编码服务地址：外层 `service` 指向登录网关，内层 `fromurl` 明确返回 `https://onlineweb.zhihuishu.com/`。自动或人工登录完成后可直接落到“我的学堂”，减少先回首页再依赖入口按钮的竞态；登录主机识别和旧版 DOM 契约保持兼容。测试会解析两层查询参数并验证门户回跳目标，避免手写百分号编码失真。

85. 课程门户加载完成后不再固定休眠 3 秒再判断结果，而是通过 `asyncio.Event` 等待实际成功解析的课程接口响应；快速网络立即继续，慢网络最多等待 20 秒，超时后明确区分“网络缓慢或接口变化”并保持旧数据。该等待包含独立的成功与超时回归测试，避免页面渲染速度变化导致随机误报。

86. 智慧树课程访问标识集中由 `get_zhs_course_access_id` 解析，同时兼容历史 `secret` 与当前接口可能直接提供的 `recruitAndCourseId` 字段；抓取脚本会把二者归一为同一缓存格式，课程目录再统一生成编码后的运行链接。完全缺少访问标识的课程不会进入 Web 选择列表，避免出现默认勾选但确认后无法添加的空链接课程。

87. 课程响应捕获不再只绑定 `queryShareCourseInfo` 固定方法名，而是在精确的 `onlineservice-api.zhihuishu.com` 主机和包含 `course` 的 API 路径范围内按响应结构确认 `courseOpenDtos`。接口路径改名但数据契约保持时仍可自动获取课程；相似恶意域名、用户资料接口和其他非课程响应不会被误判，旧接口的解析异常仍保留明确日志。

88. Web 获取智慧树课程时把成功空列表作为正常业务状态处理：按钮显示“暂无课程”，提示改为信息级“当前账号暂无可刷课程”，不再先显示成功再弹出警告。课程数组会先做类型归一化，有课程时仍进入选择弹窗，空课程时不会修改现有课程链接；按钮样式在 3 秒后完整恢复。
89. Web 课程选择弹窗改为“保留已有配置并去重追加”，不再用本次勾选结果覆盖原有课程。智慧树按 `recruitAndCourseId`、`secret` 或 `liveId` 课程身份去重，学习通按规范化后的课程名去重；确认后同步显示本次新增量和总数，关闭弹窗时清理临时引用，避免旧账号选择状态泄漏到下一次操作。回归覆盖两个平台的追加写入契约。
90. 智慧树与学习通课程选择统一接入 Web 配置保存结果：只有后端确认保存成功后才显示添加成功；保存失败时保留页面中的新选择并明确提示“尚未保存，请重试”，不再吞掉异常或同时出现成功与失败提示。学习通从原先仅修改文本框、依赖用户再次手动保存，改为确认选择后立即保存。
91. 学习通课程接口增加响应结构契约校验：只有包含列表型 `channelList` 的响应才可进入解析与缓存，其中空列表仍代表合法的“暂无课程”；登录失效、接口报错或改版后缺失字段的 JSON 不再被误报为空课程并缓存 30 分钟，而是返回“可能登录已失效或接口已变化”。回归同时证明合法空列表可缓存、异常响应不会落入缓存。
92. 智慧树课程抓取的事件等待补充响应稳定窗口：收到第一条合法课程响应后不再立即关闭浏览器，而是在 0.75 秒内持续接收并合并后续课程分类响应；每次新响应都会重新开始稳定等待，同时仍受 20 秒总上限约束。这样既保留事件驱动的快速完成，也让已有的多响应去重合并逻辑真正覆盖第一批之后的数据。回归用连续事件证明稳定窗口会被正确延长并有界结束。
93. 智慧树课程响应进一步收紧字段有效性：真正的空 `courseOpenDtos` 仍代表“暂无课程”，但非空数组只要包含非对象行，或所有课程都缺少已知的 `secret` / `recruitAndCourseId` 访问标识，就判定为接口字段变化并保留旧数据，不再过滤后误写为空课程。回归覆盖缺失访问标识与非法行类型。
94. Web 与后端配置服务统一校验 Autovisor 课程链接：只允许智慧树官方域名及其子域名的 HTTPS 地址，外部站点、HTTP、伪造后缀和无效文本会在保存前指出具体账号与行号，不再等到核心启动后静默忽略。配置落盘时按课程访问身份去重，历史 `secret` 与新版 `recruitAndCourseId` 的相同值视为同一课程，见面课按 `liveId` 单独去重，保持首次出现顺序。回归覆盖前后端契约、伪造域名、跨字段重复，以及后端在任何配置文件写入前拒绝非法链接。
95. Autovisor 启动增加独立的运行前配置门禁，保存空账号仍被允许，但点击启动时单账号必须存在课程链接，多账号模式会忽略完全空白的占位卡，并逐一指出已填写账号但缺少课程的卡片。该门禁在浏览器检测、依赖安装、题库准备和后台线程创建之前执行，因此当前无课程账号不会再短暂显示“启动中”后由核心自行退出；Web 动作可直接收到稳定的拒绝原因。回归证明无课程时不会进入运行环境准备阶段。
96. 核心子进程异常退出会写入对应的 Web 可见日志流，Autovisor 与刷题模式的非零返回码、输出读取异常和监控异常均可由活动摘要稳定显示为“运行失败”，并保留经过脱敏和长度限制的错误详情；用户主动停止仍保持安静。自动关机条件同时收紧为“核心正常返回且全部任务结束”，异常退出只通知错误，绝不会触发“刷完自动关机”倒计时。回归覆盖正常完成、异常退出、输出失败和自动关机分流。
97. 自动关机安全状态从“最后退出的单个核心”扩展为整轮任务维度：一键启动的任一核心启动失败或运行异常都会保留本轮失败标记，即使另一个核心随后正常完成且所有进程已停止，也只记录“已跳过自动关机”，不会关机。全新独立启动会在确认系统空闲后重置上一轮标记；一键启动期间的快速失败不会被后续核心启动竞态清除。回归覆盖混合失败/成功退出、部分启动失败和空闲新轮次重置。
98. Web 用户主动点击“获取课程”会为智慧树与学习通显式启用强制刷新，跳过身份匹配的 30 分钟课程缓存并访问最新接口；内部或未来的非交互调用仍默认使用缓存。这样账号从“暂无课程”变为新开放课程后无需等待缓存过期，重复点击即可获取最新列表。回归证明已缓存的智慧树空结果会被新课程覆盖、学习通强制刷新会重新登录/请求，而默认调用仍保持缓存命中。
99. 学习通课程获取补齐与智慧树一致的合法空状态和后端并发门禁：成功空数组时按钮显示“暂无课程”，使用信息级提示且不打开选择弹窗、不修改既有课程筛选；重复 API 调用会在登录和网络请求前返回“正在进行中”，避免同一共享会话并发认证。按钮保持短暂结果状态后再恢复，回归覆盖前端空状态与后端单实例拒绝。
100. 智慧树课程数据的刷新时间提升到微秒精度。后端以课程文件内容指纹判断抓取脚本是否完成刷新，旧版秒级时间会让同一秒内两次内容相同的合法结果（尤其“暂无课程”）产生相同文件并被误报为“未刷新”；现在每次实际落盘均能形成新的刷新指纹，回归固定在同一秒内连续保存并验证内容与时间标记均变化。
101. Web 课程获取按钮补齐异步结果期防重入与旧计时器隔离：智慧树成功或空课程结果展示的 3 秒内保持按钮禁用，避免立即重试时旧计时器覆盖新请求的“获取中”状态；两个平台的恢复计时器均只操作创建时捕获的按钮节点，并在节点仍连接页面时才恢复，不再按相同 ID 误改页面重绘后的新按钮。
102. 视频后台任务补齐异常分流和限频诊断：窗口最小化恢复、倍速/静音调节及暂停恢复不再对所有未知异常静默重试；正常的 Playwright 元素等待超时继续安静处理，浏览器关闭仍立即退出，其他异常会在首次及每连续 5 次写入 Web 可见警告。全国共享课播放状态读取也并入同一分流，站点字段变化不再被内部 `pass` 吞掉。

103. 完成“洛天依”Web 助手板块核对：AI 对话、自定义模型配置、本地快捷指令、主题解锁和本地图片资源均接入现有 WebView。启动/停止 Yatori、Autovisor 与题库的快捷指令现在会透传后端真实结果，保存或启动失败时显示红色失败消息，不再误报“指令已发送”；自定义 AI 服务缺少地址、模型成功响应为空时会返回稳定错误。新增前端指令契约和本地资源存在性测试，并忽略 Yatori 运行日志，避免日常使用持续污染版本库。
104. 章节视频与课程进度读取补齐改版故障保护：章节列表整体读取失败或全部条目不可读时会终止本章并写入原因，不再用空列表制造“没有视频、章节完成”的假成功；单个异常卡片仍可隔离后继续扫描，警告按首项和每累计 5 项输出。见面课签到文本支持从“签到进度 80%”等装饰文本提取百分比，视频状态统一校验无穷时长、加载状态和页面跳转过渡态，新版普通课在进度文字暂时缺失时也会检查完成图标；浏览器关闭继续向上退出，其他进度读取异常通过学习循环共用的 30 秒限频诊断进入 Web 日志，不再无声降级为 `0%`。同时清理了从未调用的章节 DOM 题目提取兼容代码。
105. 纯 Web 退出生命周期补齐失败回退与竞态保护：系统关闭事件只会排队一个非阻塞确认弹窗；弹窗脚本缺失、渲染器异常或延迟任务无法创建时会记录具体原因，并按用户本次关闭请求回收课程获取、核心和题库后安全退出，不再静默拦截窗口或留下后台进程。前端确认按钮改为等待 `exit_app` 桥接结果，失败时恢复弹窗并显示原因，成功后只由后端完成清理和窗口销毁，不再同时调用 `window.close()` 产生二次关闭竞态。
106. Web 成就系统接入配置失败、核心异常、首次启动、双核运行、题库数据和洛天依指令等日常事件；Yatori 空账号启动会在创建核心进程前拦截并作为配置类失败返回给前端，批量启动会聚合配置失败类型，避免一键启动继续误导。成就偏好支持旧数组格式、旧 `tianyiAchievementShown` 标记和新版对象格式统一归一，远端重置令牌高于本地时会按后端状态清空，防止重置后被本地缓存合回。新增 `scripts/reset_achievements.py` 与 `重置成就状态.cmd` 方便关闭启动器后清空成就并创建备份，备份文件已加入忽略规则。

107. Web 日常使用失败诊断继续补齐：学习通登录 302 跳转探测失败时会记录具体原因并继续尝试课程列表，避免 Cookie 已有效却只剩无声降级；题库服务启动后端口未就绪且清理失败时会把清理异常写入日志，避免失败实例残留原因不可见；启动器提示音播放失败会限一次写入系统日志，用户开启声音提示但系统音频不可用时能看到原因且不会刷屏。新增测试覆盖三条失败路径的可恢复性、状态清理和日志限频。
108. Dashboard 的 `/api/logs` 接入真实任务子进程输出：`TaskSupervisor` 写入的 `DashboardTask.log` 现在会按尾部读取并合并到 Web 日志流，保留原有 `limit` 与 `level` 过滤；普通子进程输出标为 `TASK`，包含 Traceback/Exception/错误/失败的行会归类为 `ERROR`，日志文件读取失败会给出 Dashboard 警告而不是静默空白。新增 API 测试覆盖任务日志尾部显示和错误等级过滤。
109. Dashboard 的 `/api/stats` 不再返回固定 92% 成功率；会从 Dashboard 内存日志和 `DashboardTask.log` 尾部推导已知会话数、视频完成数、测验完成数和成功/失败比例。没有终态证据时 `success_rate` 返回 `null`，前端显示 `--`，避免把未知状态渲染成假百分比。新增测试覆盖默认未知成功率、任务日志推导统计、成功/失败比例和前端占位显示。
110. Dashboard 的 `/api/courses` 不再只返回固定 `0/pending` 课程行；会从任务日志尾部识别“开始处理第 N/M 门课程”、当前课程标题、播放进度、单课程执行完成/失败和全部课程完成信号，把对应配置 URL 行更新为 running/completed/failed 及真实百分比。没有日志证据的课程仍保持 pending，避免猜测平台字段。新增测试覆盖当前课程进度、单课程完成/失败和全部完成三种课程表状态。
111. Dashboard 日志推导口径收紧：相同运行日志只作为一条证据计数，100% 进度和“视频完成”两类视频完成证据取较大值而不是相加，重复终态日志不会抬高成功/失败次数；课程表同一课程只保留最高播放进度，没有明确执行完成/失败或全部完成信号时不再因进入后续课程而猜测前序课程 completed。新增测试覆盖重复日志去重、100%/视频完成不重复累加、课程进度不回退和非明确完成不误标。
112. Dashboard 任务日志补充真实会话边界：`TaskSupervisor` 在子进程启动日志中写入 `[DASHBOARD_SESSION_START] session_id=...`，启动失败会回滚该边界，`TaskSnapshot`、启动响应和状态响应均暴露 `session_id`；`/api/logs`、`/api/stats` 和 `/api/courses` 在发现会话边界时只读取最新会话后的子进程输出，避免旧任务的完成/失败/进度污染当前 Dashboard。新增测试覆盖会话边界写入、启动失败不残留边界、日志只读最新会话，以及旧会话不污染统计和课程表。
113. 洛天依本地控制指令补齐前端互斥：执行启动/停止/题库等控制命令期间会设置 `commandBusy`，重复发送会显示“上一条控制指令还在执行”的失败消息而不会再次调用后端，避免连按 Enter 或重复点击导致双启动/双停止。AI 对话触发的明确控制指令仍能复用当前忙碌态，不会提前解除聊天加载状态。新增前端契约测试覆盖互斥字段、执行前置锁和 `finally` 释放。
114. 智慧树课程抓取补充录制响应契约夹具：新增当前 `recruitAndCourseId` 课程响应、旧 `secret` 混合响应和见面课通知响应样本，测试覆盖普通课、共享课、旧字段、见面课 URL 生成和通知形状校验。课程接口返回同批次混入缺少访问标识的脏行时，现在会过滤该行而不是生成不可运行课程；通知接口若不再返回对象列表，会显示“无法识别”并跳过写入，避免把改版脏数据保存进课程文件。
115. Yatori 更新检查改为优先读取 GitHub releases 列表并选择最新可安装的 Windows ZIP，包含 beta/prerelease 版本；`/releases/latest` 仅作为兼容回退。这样 Yatori 发布 beta.11/beta.20 之类预发布版本时，不会继续被旧 `latest` 路径或本地 `v2.6.2-beta.8` 兜底卡住。新增测试覆盖发布列表优先级、draft 跳过、beta 选择和 GitHub 资产摘要元数据保留；Autovisor 因项目内已有魔改，仅保留版本信息检查，上游覆盖安装由项目策略明确禁用。
116. 修复 Yatori 本地版本记录缺失时的误判：本地已存在核心文件但 `.core_versions.json` 没有 `yatori` 版本时，不再直接返回“无需更新”，而是使用内置兜底版本 `v2.6.2-beta.8` 与 GitHub 返回的最新版本比较；若远端为 `v2.6.2-beta.11` 等更新版本，会进入 Web 确认弹窗，用户确认后才开始安装。Web 管理中心与自动检查日志现在会显示“当前版本 → 最新版本”，并截取 GitHub Release 正文作为“Yatori 最新版本介绍”，方便确认更新内容而不是只看到版本号。新增测试覆盖缺失版本记录、手动检查安装日志和已是最新版本时的版本介绍弹窗。
117. Yatori 更新按钮改为确认式交互：点击“更新/安装 Yatori 更新”后，后端只检查并返回最新版本、发行资源、发布时间和版本介绍；前端展示专用确认弹窗，用户点击“确认更新”后才调用 `install_yatori_update` 开始下载和替换核心文件。这样可以在更新前先阅读 beta 版本变更内容，也避免误触按钮导致自动覆盖。新增测试覆盖 Web 动作不提前安装、确认动作才启动安装，以及前端按钮/弹窗的静态契约。
118. 新增 `desktop_platform_service.py`，把开机启动项、旧日志清理、窗口最小化、提示音和 Windows 自动关机从巨型启动器中拆出，并保留原 Web 调用入口。自动关机和取消关机现在会检查 Windows 命令真实返回码：系统拒绝或超时时不会再把 `_shutdown_pending` 错标为成功，也会把具体失败原因写入 Web 系统日志。新增测试覆盖启动项原子创建/删除、日志保留边界、提示音异常限频、关机接受/拒绝和取消失败后的状态保持。
119. 新增 `web_window_controller.py`，WebView 窗口绑定、退出弹窗互斥、关闭事件分流和窗口销毁从启动器中拆出；窗口销毁失败会恢复安全关闭拦截，退出清理异常会留在窗口内并写入原因，窗口状态保存失败也不会阻止后续安全退出。日志导出和本地路径打开并入桌面平台服务：日志类型采用白名单，同一时间及并发导出会生成不同文件，资源管理器打开失败时仍返回“文件已成功导出”及真实路径，不再误报整体失败。新增测试覆盖弹窗合并、缺失弹窗回退、清理/销毁/几何状态异常、非法日志类型、重复与并发导出、外壳打开失败和空路径拦截。
120. 新增 `runtime_coordinator.py`，一键启动互斥、跨核心失败批次、运行通知、结构化异常事件和自动关机收尾从启动器中拆出。自动关机现在要求全部 `running` 与 `starting` 状态（包含刷题模式）均空闲；异常核心无论先退出还是最后退出都会稳定跳过关机，异步进程创建/输出线程失败也进入同一收尾，不再受退出顺序影响。新任务启动前会先取消上一轮待执行关机，取消失败时阻止新核心启动；Windows 返回码 1116 按“本来没有关机任务”清除陈旧状态。一键启动后端增加非阻塞互斥，重复请求不会并发启动题库和核心；批次仍在拉起其他核心时不会提前清除已记录失败。Web 成就观察同时识别 `runtime_failure` 结构化事件。新增测试覆盖异常最后退出、启动中核心/刷题模式阻止关机、待关机取消成功/失败、陈旧状态恢复、批量重复请求、通知偏好和异步启动异常收尾。
121. 新增 `core_launch_service.py`，Yatori 与 Autovisor 的配置门禁、入口选择、Python 依赖准备、题库连接准备和进程提交从统一启动器组合入口拆出，主文件缩减到约 1,630 行。Autovisor 仅缺 Playwright 浏览器时不再输出空的“缺少依赖”提示，依赖准备改用明确的 ready/installing/failed 状态，避免同步测试或快速完成时误判。Web 运行状态新增 `dependency_installing`，核心卡片在进程创建期间显示“启动中/取消启动”，点击会发送停止操作；依赖安装期间显示“准备环境/安装依赖中”并禁用重复点击。新增服务单元测试和前端契约测试覆盖浏览器单独缺失、安装任务拒绝、EXE 跳过 Python 检查与状态分流。
122. Web 动作服务统一失败结果契约：启动、停止、更新、题库、未知动作和动作异常都会尽力附带最新完整状态，前端显示错误后同步卡片，不再停留在操作前的陈旧状态；状态快照本身失败时只记录诊断，不会把已经成功的动作改判为失败。Autovisor 上游覆盖安装按项目本地魔改约束增加双层保护：中控台与“关于”页均保留版本检查，但安装按钮替换为禁用的“本地适配版”；旧页面或直接桥接调用 `install_autovisor_update` 也会由后端明确拒绝，启动器兼容方法不会触达上游安装器。版本检查日志改为“信息仅供参考”，新增测试覆盖策略拦截、不调用安装器、错误状态保真和状态刷新异常隔离。
123. 新增 `application_bootstrap.py`，未捕获异常日志、pywebview 文件选择常量兼容、DPI 设置、控制台隐藏、窗口创建与 WebView 启动从主入口拆出，`main()` 从约 78 行缩减到 30 行，重复的二次崩溃日志写入路径被移除。窗口创建或 `webview.start` 失败时会以确认退出语义回收已经初始化的启动器；清理自身失败只记录诊断，不会覆盖最初的启动异常。启动器的延迟回调改为可追踪集合，题库自动启动、更新检查和自动运行在正常退出或启动失败时会被统一取消；未确认且仍有核心运行的退出请求不会误取消这些任务。新增测试覆盖主入口装配、新旧 pywebview 常量、创建/启动失败清理、清理异常保真、单份崩溃日志，以及定时任务完成移除与退出取消。
124. 新增 `web_settings_service.py`，Yatori、Autovisor 与题库三组 Web 配置的输入归一化、隐藏字段合并、跨文件快照、保存顺序和失败回滚从主启动器拆出，主文件缩减到约 1,520 行。Autovisor 非对象账号和 Yatori 非对象嵌套配置会在任何写入前返回稳定的具体错误，不再在校验前抛出桥接异常；归一化使用副本，不会修改调用方 payload。题库设置可能重启实时服务，因此调整为事务最后一个可失败步骤，前面任一步或题库自身失败都会恢复两份核心配置、题库配置与多账号运行模式。配置实际保存成功但状态快照临时失败时，后端返回 `ok=true/stateUnavailable=true`，前端显示绿色“已保存但状态暂未刷新”，不再误报保存失败。新增测试覆盖输入不变、隐藏字段保留、写入顺序、三文件回滚、异常输入和状态刷新失败分流。
125. Yatori 覆盖安装改为可回滚事务：下载和 SHA-256 校验后先在独立临时目录解压，递归定位实际可运行入口并完整暂存；只有暂存校验通过才把旧核心移动到同盘备份并激活新目录，激活或版本记录写入异常都会恢复旧核心，不再先删除旧版本后逐文件复制。用户配置只从当前 `Yatori/config.yaml` 内存快照恢复，不再复用根目录固定 `config.yaml.bak`，避免已删除或已更换账号后被陈旧备份覆盖。版本记录也改用原子 JSON 写入，发行标签先转换为安全临时目录名。新增测试覆盖嵌套发行包、当前配置保留、陈旧备份忽略、无入口发行包拒绝、激活失败回滚及版本记录失败回滚。
126. 智慧树课程抓取不再因通知接口稍晚、缺失或改版而清空已有见面课：课程响应稳定后会为官方通知接口保留 2 秒有界窗口，通知地址采用官方主机与 `importantNotice` 路径双重约束；只有收到可信列表（包含合法空列表）才覆盖 `notices`，未收到可信响应时原子更新普通课程但保留当前账号已有通知，损坏的非对象顶层课程文件会明确重建。全国智慧共享课的视频打开、课程列表恢复和返回导航统一复查 URL+DOM 登录状态；登录失效或页面关闭作为致命错误传播，普通返回失败作为课程失败传播，不再继续扫描登录页或误报“视频播放完成，已返回课程列表”。智慧树站点与首页判断也改为精确主机边界，避免查询参数或相似域名误判。新增测试覆盖通知端点边界、有界等待、旧通知保留/合法清空、损坏顶层重建、视频登录跳转与返回失败不误报。
127. 新增 `web_state_service.py`，Web 运行状态与初始状态聚合从统一启动器拆出，主文件缩减到约 1,470 行。`running` 与 `starting` 现在在共享运行锁内一次复制，避免轮询恰逢进程切换时生成互相矛盾的卡片状态；日志字符串按行归一而不再被 `list()` 拆成单字。版本、核心安装探测、依赖状态、题库统计和活动摘要等非关键探针各自故障隔离，单项失败会提供稳定默认值与 `state_issues`，并按组件 30 秒限频写入系统日志，Web 后端仍保持连接；运行事件也复制后交给前端，避免并发修改。配置读取仍保持严格失败语义，防止用空默认值掩盖失败后被用户保存覆盖。新增测试覆盖完整快照、持锁复制、日志归一、题库/版本/安装探针降级、诊断限频与初始配置委托。
128. 新增 `scheduled_task_service.py`，把启动阶段延迟任务的守护线程创建、待执行集合跟踪、完成移除和退出取消从统一启动器拆出，主文件缩减到约 1,460 行。后台回调异常不再静默消失，会记录回调名称与失败原因并可靠移出集合；定时器构造或启动失败不会残留不可取消任务，单个任务取消失败也不会阻止其余任务清理。零延迟调用继续同步执行并传播异常，保持原有调用契约。新增测试覆盖跟踪完成、回调异常、启动失败、取消异常隔离与零延迟语义。
129. 新增 `launcher_startup_service.py`，把桌面平台、偏好、核心路径、依赖管理、题库、CoreManager 与更新控制器的有序装配，以及题库自启、核心更新检查和自动运行三个延迟任务声明从主构造器拆出，主文件缩减到约 1,420 行。可选 CoreManager 缺失时仍会创建只读更新控制器，题库模块可用与缺失提示保持不变，多账号模式继续由规范化配置推导；启动任务继续按 `600/1000/1400ms` 原顺序注册。新增独立测试覆盖服务参数与回调传递、日志自动清理、双账号模式、可选依赖缺失和延迟任务顺序。
## 仍需优先处理的问题

### P2：维护性与可观测性

- `统一启动器.py` 仍约 1,420 行；Windows 平台副作用、应用启动、窗口退出、日志导出、Web 动作、配置事务、状态聚合、启动服务装配、延迟任务调度、跨核心批次协调和两套核心启动细节已拆分，剩余兼容转发与基础状态初始化仍应继续收敛。
- 大量 `except Exception: pass/continue` 会吞掉选择器变化和状态错误。应只忽略已知的 Playwright 超时/页面关闭，并给未知错误加限频日志。
- 根目录的 `绘图.py` 已在本轮功能改动中清理；`Untitled-1.py` 仍与项目业务无关，建议确认后移入独立实验目录或删除。
- GitHub 资源摘要已保护自动更新，但项目尚未验证独立发布方数字签名或 Sigstore 证明；需要更强供应链身份保证时仍应补充独立证明。

## 推荐的下一轮拆分顺序

1. 继续补录智慧树登录页、课程门户页和播放器页 HTML 契约样本，覆盖页面改版而不修改真实账号状态。
2. 将 `UnifiedLauncher` 中成组的兼容转发按子系统收敛，降低主类接口噪声并保留旧调用契约。
3. 将 Dashboard 的日志推导继续演进为结构化运行事件，并在可验证真实课程接口时接入平台课程字段。
4. 在 GitHub SHA-256 之外增加独立发布方签名或 Sigstore 证明（增强项）。

## 验证状态

- 本轮全量回归为 530 项测试和 7 项子测试全部通过；启动服务装配顺序、回调参数传递、可选核心管理器缺失、题库状态提示、多账号模式与延迟启动任务顺序相关测试通过，Python/JavaScript 语法和 Git 差异格式检查通过。
- 本地真实浏览器完成 Yatori 确认式更新回归：点击“更新”后调用序列只有 `show_update_dialog`，弹窗正确显示 `v2.6.2-beta.8 → v2.6.2-beta.11`、发行资源、发布时间和两行版本介绍；点击“确认更新”后调用序列才增加 `install_yatori_update`。测试使用模拟后端，没有下载或替换真实 Yatori；页面仅有本地静态服务器缺少 favicon 的无关 404。
- 本地真实浏览器完成配置保存降级反馈冒烟：页面向模拟后端提交 Yatori、Autovisor 和题库三组配置（账号数各 1、题库端口 8083）；模拟后端返回 `ok=true/stateUnavailable=true` 后，页面显示绿色 `toast success`“配置已保存，但界面状态暂未刷新”，没有误报保存失败。页面无脚本异常，仅有本地静态服务器缺少 favicon 的无关 404；测试没有写入真实配置、账号或课程。
- 本地真实浏览器完成启动入口拆分后的资源冒烟：主界面标题、Yatori、Autovisor、题库和控制台均完整渲染，切换“核心设置”后常规设置、AI 配置和账号管理控件可见，证明 HTML、样式和脚本资源路径保持有效。页面无脚本异常，仅有本地静态服务器缺少 favicon 的无关 404；测试未连接真实后端、账号或课程。
- 本地真实浏览器完成 Autovisor 更新入口保护复验：中控台显示可点击的“检查版本”按钮，原“安装”按钮显示为禁用的“本地适配版”；“关于”页保留“检查 Autovisor 更新”，覆盖安装入口显示为禁用的“Autovisor 本地适配版”。两个入口均不可点击覆盖，页面无脚本异常，仅有本地静态服务器缺少 favicon 的无关 404；测试未联网检查版本，也未修改真实核心。
- 本地真实浏览器完成核心启动状态冒烟：Yatori 模拟进入启动中后卡片显示“启动中/正在启动”，按钮变为“取消启动”，点击后模拟后端只收到一次 `stop, yatori`；Autovisor 模拟依赖准备时显示“安装依赖中/准备环境”，操作按钮禁用。页面无脚本异常，仅有本地静态服务器缺少 favicon 的无关 404；测试未启动真实核心、账号或课程。
- 本地真实浏览器完成 Yatori 更新确认交互冒烟：第一次点击更新只调用 `show_update_dialog`，弹窗显示 `v2.6.2-beta.8 → v2.6.2-beta.11`、发行资源、发布时间和两行版本介绍；“稍后再说”关闭弹窗且不调用安装，再次打开后只有点击“确认更新”才调用一次 `install_yatori_update` 并关闭弹窗。测试使用本地模拟 WebView 后端，没有下载、替换或启动真实 Yatori 核心。
- Autovisor 因项目内已有魔改，上游版本检查仅作为信息参考，Web 和兼容后端入口均禁止覆盖安装；更新本地适配版时应先人工合并上游变更并重新执行回归。
- 本地系统 Chrome 在 `1180×760` 下完成成就弹窗冒烟：旧数组格式成就从本地偏好加载后按钮计数为 2，弹窗可打开，7 个成就项全部渲染，其中 2 个为已解锁、5 个为未解锁，页面无脚本异常。测试使用本地静态 Web 页面和干净浏览器上下文，未访问真实账号或课程。
- 本地系统 Chrome 完成退出交互复验：退出弹窗可打开、取消和再次确认；模拟后端失败时弹窗恢复并显示原因，重试成功后 `exit_app` 总计按点击调用两次，浏览器原生 `window.close()` 调用为 0，页面无脚本异常。
- 本地系统 Chrome 在 `1180×760` 下再次完成只读启动冒烟：启动器标题和洛天依导航唯一命中，页面切换成功，图片无缺失、无脚本异常和水平溢出。本轮没有真实课程，章节列表与进度改版故障通过模拟页面关闭、坏卡片、签到文本、视频过渡态和重复异常的自动测试验证。
- 本地系统 Chrome 在启动器最低支持窗口 `1180×760` 下完成“洛天依”页面交互复验：所有图片资源加载成功，无脚本错误、无水平溢出；模拟题库启动失败时页面显示红色失败消息及后端原始原因，不再显示成功。测试使用本地模拟 WebView 后端，未访问真实账号、课程、核心进程或外部 AI 服务。
- 自研 Python 文件均通过 Python 3.13 语法编译检查。
- 仓库根目录收集的 395 项测试和 7 项子测试全部通过，覆盖题库导入/导出/去重/清理/ZError 探测/连接释放、更新控制器重复检查/代理显式开关/展开资源解析/无虚假降级版本/GitHub 资源摘要元数据/下载前安全拦截/文件大小与 SHA-256 校验/未校验更新显式覆盖/旧核心保留/运行中拦截/成功安装/异常复位/陈旧候选清理、Autovisor 依赖描述兼容/镜像回退/并发安装/失败复位/浏览器配置原子写入、题库控制器设置加载/端口校验/密钥不落盘/失败回滚/端口重启失败后旧服务恢复/运行配置回调异常回滚/失败清理、纯 Web 启动契约/真实失败反馈/题库运行文案同步/无 Tk 依赖维护入口/requirements 分组一致性/解释器与镜像传播/独立 Web 动作分发与异常反馈/独立 AI 服务输入验证/延迟加载/稳定异常结构/密钥日志保护/独立偏好服务兼容加载与事务回滚/断连临时保存提示/批量启动部分失败/导入取消/去重失败/危险操作确认/初始化重试/轮询互斥/陈旧状态拒绝/跨配置文件事务回滚/保存失败拦截/窗口绑定/文件选择/退出确认、前后端 API 映射、启动器进程状态/并发占位/输出解码/进度覆盖/终止回退/核心后台生命周期取消与孤儿清理/刷题模式独立编排/题库启动失败拦截/原子启动和监控失败清理/多账号刷题选择与 Cookie 隔离/刷题异常退出码传播/刷题按钮真实状态与卡片停止/刷题浏览器显式回收/刷题恢复异常限频诊断/刷题页面关闭与未知异常分流/测验页扫描和渲染诊断/监听器异常清理/测验监听注册与回调故障隔离/打包解释器发现、Autovisor 运行状态解析/敏感查询参数清理/Web 进度卡片契约、YAML/INI 特殊字符往返/损坏回退/未知字段保留/动态账号编号与课程身份映射、课程缓存身份隔离/内容刷新/身份错配/异常进程清理/过期损坏处理/HTTPS 请求/会话切换/历史文件拒绝、题库 HTTP 策略、答案解析与点击动作、测验有界并发查询/单题异常隔离/浮窗参数传递、答题页点击回退/翻页失败/真实作答状态/交卷完成判定/慢提交轮询/手动交接未确认、随堂题题库优先/单双选回退/课程切换/完成信号、页面文本转义/重建/安全验证生命周期/状态 OCR 限频诊断/上下文关闭失败/精确文本回退原因、Dashboard 真实子进程监督/API 生命周期、日志路径/并发落盘、配置归一化、账号隔离、并发队列、自定义题库 URL、课程类型映射、课程会话、课程门户导航/子页面恢复/失败停止、多课程失败隔离/致命错误停止/后台任务回收、卡片导航状态、全国共享课测验会话/真实提交结果传播/失败重试、四类课程执行流程、视频真实完成传播/停滞恢复上限/播放位置判定/准确进度显示、视频暂停恢复/页面关闭/异常限频诊断、课程弹窗扫描失败可见性、章节视频筛选、测验响应抓取生命周期、新旧登录页选择器契约、登录 URL 与 DOM 双重识别、协议自动勾选和歧义拦截、表单隐藏后的跳转确认、课程获取共享门户导航、账号 Cookie 复用、可见人工验证回退、浏览器偏好与敏感日志脱敏、自动/手动登录等待边界、超时失败与凭据保存时机、Web 课程选择保留与去重追加、学习通空课程与异常响应分流、智慧树多响应稳定聚合、课程链接前后端校验与跨字段去重、Autovisor 无课程启动前拦截、异常退出可视化与自动关机安全分流、Web 主动课程刷新绕过陈旧缓存、学习通空状态与课程请求单实例保护、同秒课程刷新指纹更新、课程结果按钮防重入与旧计时器隔离、视频后台任务未知异常限频可见性。
- 单账号与多账号 CLI 冒烟通过，`--help` 不再触发题库网络连接。
- Chrome 本地只读冒烟确认 Web 前端正常载入：标题为“统一启动器 2026”，中控台、Yatori、Autovisor、题库和系统控制台均可见，样式与脚本资源加载完成；核心设置导航可切换且 48 个配置控件完成渲染。静态页面显示“正在连接后端”符合未注入 pywebview API 的预期；控制台未发现本地页面脚本错误，仅有 Tailwind CDN 的生产环境提示。
- Chrome 题库安全冒烟确认题库设置页、服务状态、AI 配置和四个数据操作按钮正常渲染；危险清空按钮会先出现原生确认框，测试未接受确认且未调用清空后端。按钮文案也已从误导性的“仅清空 AI 缓存”改为与实际行为一致的“删除所有本地题目数据”。
- Chrome 断连冒烟确认页面加载后会主动尝试初始化；在未注入 pywebview API 的预览环境中，3 秒后状态切换为“请通过统一启动器打开此界面”并仅显示一次说明提示，持续重试期间无本地脚本错误。
- Chrome 插件已恢复连接，并使用真实 Chrome 对新版智慧树公开登录页完成只读复验：`#login_center_app .login-container`、`input[name="mobile"]`、密码输入、协议复选框和 `.btn-block__grandient_login` 均唯一命中 1 个；旧版 `.wall-main` 与 `#lUsername` 均为 0，协议初始状态为未勾选。未读取或填写输入内容、未勾选协议、未点击登录，也未启动课程或提交内容；真实登录后的课程门户、播放、签到和答题仍需在用户登录会话中继续验证。
- Chrome 直接打开未登录的课程门户时，浏览器先提交 `onlineweb.zhihuishu.com` 导航，随后新版登录面板完成渲染并把地址更新到 `login.zhihuishu.com`；检查时登录根节点和面板各命中 1 个，课程容器均为 0。该证据用于验证 URL 与 DOM 双重登录识别；未提交账号或改变站点状态。
- Chrome 对智慧树首页课程入口完成只读复验：“我的学堂”文本链接和指向 `https://onlineweb.zhihuishu.com` 的链接均唯一命中 1 个，当前共享导航优先使用该精确目标；核验未点击入口、未读取账号信息，也未改变课程状态。
- 本地真实 Chrome 通过 Python Playwright 完成 Web 课程选择交互冒烟：同一 `recruitAndCourseId` 的旧课程默认识别为重复项且不勾选，新课程确认后保留原链接并去重追加；模拟后端保存失败时新选择仍留在页面，没有成功提示，只显示“尚未保存，请重试”。两条路径均未出现页面脚本异常，测试使用本地模拟课程与保存结果，未访问或修改真实账号。
- 本地真实 Chrome 复验课程 URL 契约：两个智慧树 HTTPS 地址均通过，HTTP 地址、伪造的 `zhihuishu.com.example.com` 和 `javascript:` 地址均被拒绝；旧 `secret` 与新版 `recruitAndCourseId` 使用相同值时，弹窗把新版链接识别为已存在，只默认勾选新的见面课链接。页面无脚本异常，未访问真实站点。
- 本地真实 Chrome 复验运行失败卡片：注入非零退出摘要后，活动面板保持可见，`data-phase` 为 `failed`，标题显示“运行失败”，详情显示“Autovisor 已退出，返回码: 3”，无进度条且无页面脚本异常。
- 本地真实 Chrome 复验学习通空课程交互：模拟保存成功且课程接口返回空数组后，按钮显示“暂无课程”并保持短暂禁用，页面只出现信息级“当前账号暂无可刷课程”，没有警告或选择弹窗；原有“只刷这些课程”文本保持不变，页面无脚本异常。
- 本地真实 Chrome 复验课程按钮竞态：智慧树与学习通返回空课程后按钮在结果展示期均保持禁用；切换配置页触发账号卡片重绘后立即发起第二次慢请求，第一请求遗留的恢复计时器不会启用新按钮或覆盖“获取中”状态，两个平台均无页面脚本异常。测试只使用本地模拟后端与系统 Chrome，未访问真实账号。
